appdaemon:
  threads: 10
  disable_apps: 0
  plugins:
    HASS:
      type: hass
      ha_url: !secret ha_url
      token: !secret ha_token

log:
  logfile: /var/log/appdaemon
  errorfile: /var/log/appdaemon.error
hadashboard:
  dash_url: !secret dash_url
